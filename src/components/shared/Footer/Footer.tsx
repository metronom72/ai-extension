import { Stack } from "@mui/joy";
import React, { memo } from "react";

const Footer: React.FC = () => {
  return (
    <Stack direction="row" role="contentinfo">
      asdfklasjd
    </Stack>
  );
};

export default memo(Footer);
